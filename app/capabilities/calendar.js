var mongoose = require('mongoose');

var calendar = new function() {
    
    this.createEvent = function (user, option) {
        
    }

    this.changeEvent = function (user, option) {
        
    }

    this.deleteEvent = function (user, option) {
        
    }

}

module.exports = calendar;